# План доработки магазина одежды

## 1. Личный кабинет

- [ ] Миграции:
  - `orders` (user_id, status, total_amount, shipping_address, created_at)
  - `order_items` (order_id, product_id, size, quantity, price, created_at)
  - `carts` (session_token, user_id nullable, created_at)
  - `cart_items` (cart_id, product_id, size, quantity, created_at)
  - Добавить поля в `products`: material, color, brand, care_instructions (string/text)
- [ ] Модели:
  - `Order` — belongs_to :user, has_many :order_items, enum status: { pending: 0, confirmed: 1, shipped: 2, delivered: 3, cancelled: 4 }
  - `OrderItem` — belongs_to :order, belongs_to :product
  - `Cart` — has_many :cart_items, belongs_to :user optional
  - `CartItem` — belongs_to :cart, belongs_to :product
  - `User` — has_many :orders, has_one :cart
  - `Product` — has_many :order_items, has_many :cart_items
- [ ] Контроллер `CartsController` (`show`, `add_item`, `update_item`, `remove_item`)
- [ ] Контроллер `OrdersController` (`new`, `create`, `index`, `show`) — оформление заказа из корзины, просмотр истории
- [ ] Контроллер `ProfilesController` (`show`, `edit`, `update`) — редактирование email/пароля
- [ ] Вьюхи: `profiles/show.html.erb`, `profiles/edit.html.erb`, `carts/show.html.erb`, `orders/index.html.erb`, `orders/show.html.erb`, `orders/new.html.erb`
- [ ] Обновить `_header.html.erb`: ссылки на "Профиль" и "Корзина" ведут на реальные страницы

## 2. Расширение информации о товарах

- [ ] Миграция для добавления полей `material`, `color`, `brand`, `care_instructions` в `products`
- [ ] Валидации в `Product` — поле `brand` обязательно, остальные опциональны
- [ ] Обновить `db/seeds.rb` — заполнить новые поля + добавить больше товаров (≥12) с тематическими изображениями
- [ ] Обновить `show.html.erb` — вывод новых полей (материал, цвет, бренд, уход)

## 3. Картинки

- [ ] Обновить seeds: использовать реалистичные изображения одежды с `https://images.unsplash.com/` или `https://picsum.photos/` (или по конкретным ключевым словам через Unsplash Source если доступно)
- [ ] Альтернативный вариант: `https://placehold.co` с более осмысленными текстами/цветами, чтобы избежать дропов
- [ ] Главная (about) — заменить placeholder-изображения на реальные stock-фото

## 4. Чек-лист проверки функционала

- [ ] **Главная страница (about)** — отображение, ссылка "Начать покупки" ведёт на каталог
- [ ] **Каталог** — фильтр по категории работает, фильтр по полу работает, сортировка цена по возрастанию/убыванию работает, карточки товаров отображаются
- [ ] **Модальное окно товара** — открывается через Turbo Frame, показывает изображение, описание, размеры, цену с учётом скидки, закрывается крестиком
- [ ] **Регистрация** — форма с полями email/пароль/подтверждение, валидация email, валидация совпадения паролей, toggle пароля работает, после успешной регистрации редирект на каталог
- [ ] **Вход** — форма email/пароль, неверные данные показывают ошибку, успешный вход редиректит на каталог
- [ ] **Выход** — ссылка "Выйти" работает, редирект на главную
- [ ] **Мобильное меню** — кнопка ☰ открывает/закрывает меню, ссылки работают
- [ ] **Личный кабинет** — отображение данных пользователя, возможность редактирования email и пароля
- [ ] **Корзина** — добавление товара, изменение количества, удаление, пересчёт итоговой суммы
- [ ] **Оформление заказа** — форма с адресом доставки, при пустой корзине — ошибка, после создания заказа корзина очищается
- [ ] **История заказов** — в личном кабинете отображается список заказов, можно открыть детали заказа
- [ ] **Обработка ошибок** — 404/422/500 страницы отображаются корректно
- [ ] **Флеш-сообщения** — notice/alert отображаются после регистрации, входа, выхода, оформления заказа
- [ ] **Вёрстка** — нет горизонтальной прокрутки на desktop/mobile, изображения не ломают layout
