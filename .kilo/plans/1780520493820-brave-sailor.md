# План исправления 31 issues

## Архитектура проекта
- **Rails backend** — API (`/api/v1/`) + ERB-шаблоны (каталог, админка, заказы, профиль, избранное)
- **Next.js frontend** — лендинг, каталог, карточка товара, корзина, оформление заказа, профиль
- **БД: PostgreSQL** — Active Storage для изображений

---

## 1. Лендинг: фон категорий (frontend/app/page.tsx)
- В категориях сейчас `bg-[#1a1a1a]`. Добавить background images (можно категориям добавить поле image_url или использовать узор/градиент).
- **Файлы**: `frontend/app/page.tsx`, возможно миграция БД + модель `Category`

## 2. Поле для промокода (frontend + backend)
- Добавить модель `PromoCode` (код, скидка %, активен, expires_at)
- Поле ввода промокода в корзине (`frontend/app/cart/page.tsx`) и при оформлении (`frontend/app/orders/new/page.tsx`)
- Применять скидку на `total_amount`
- **Файлы**: миграция, модель, `CartController`, `OrdersController`, страницы корзины/заказа

## 3. Избранное в frontend (frontend/app)
- Backend уже есть (`/api/v1/favorites`), но Next.js frontend не использует избранное
- Добавить кнопку "В избранное" на странице товара (`frontend/app/products/[id]/page.tsx`) и в каталоге (`frontend/app/catalog/page.tsx`)
- Страница избранного: `frontend/app/favorites/page.tsx`
- **Файлы**: `frontend/app/products/[id]/page.tsx`, `frontend/app/catalog/page.tsx`, `frontend/app/favorites/page.tsx`, API-методы

## 4. "Похожие товары" → "Может быть интересно" (frontend/app/products/[id]/page.tsx)
- Просто заменить текст на стр. 164

## 5. Уточнить размеры
- Добавить таблицу размеров/подсказку на странице товара
- **Файлы**: `frontend/app/products/[id]/page.tsx`

## 6. Блокировка заказа товаров не в наличии
- Во `frontend/app/products/[id]/page.tsx`: если товара нет в наличии (`stock_quantity === 0 || !in_stock`), кнопку "Добавить в корзину" заменить на "В избранное"
- В `OrdersController#create`: проверять `in_stock` перед созданием
- В `carts_controller.rb`: проверять `in_stock` при добавлении в корзину

## 7. Перевод ошибок в заказе
- В `frontend/app/orders/new/page.tsx`: поймать ошибки валидации с бэка и отобразить по-русски
- Обработка `catch (err: any)` — показывать конкретные ошибки, а не `err.message`

## 8. Наличие по размерам (stock per size)
- Текущая модель: `stock_quantity` на товар, `sizes` — массив. Нужно связать наличие с конкретным размером.
- Добавить модель `ProductSize` (product_id, size, stock_quantity)
- **Файлы**: миграция, модель, админ-форма, API, каталог, карточка товара

## 9. Сортировка размеров в фильтре (app/controllers/api/v1/catalog_controller.rb)
- Уже есть `.sort` на стр. 32 для `@sizes`. Проверить, что сортировка числовая, а не строковая (иначе "10" < "2")

## 10. Исправить сортировку по популярности (app/models/product.rb)
- `by_popularity` scope на стр. 49-53 крашится если нет `order_items`
- Использовать `COALESCE(SUM(order_items.quantity), 0)` с `left_joins` + `group(:id)` — проверить, что не требует `SELECT` всех полей
- Заменить на подзапрос или `.select` с нужными полями

## 11. Ограничение отзывов только после получения товара
- Добавить проверку: пользователь может оставить отзыв только на товары из доставленных заказов
- **Файлы**: `ReviewsController#create`, `Api::V1::ReviewsController#create`

## 12. Возможность оставить отзыв из профиля
- В `frontend/app/profile/page.tsx` добавить кнопку "Оставить отзыв" для каждого товара из доставленных заказов
- Или отдельный раздел "Мои отзывы"

## 13. Из заказа переход к товару
- В `frontend/app/profile/page.tsx` и ERB-шаблонах заказов сделать ссылки на товары из `order_items`
- **Файлы**: `app/views/orders/show.html.erb`, `frontend/app/profile/page.tsx`, ERB-шаблоны профиля

## 14. "Нет в наличии" в самый низ (app/models/product.rb)
- Добавить scope `in_stock_first` или модифицировать сортировку по умолчанию: `ORDER BY in_stock DESC, created_at DESC`

## 15. Проверить Обзор в админке
- `Admin::DashboardController#index` — проверить, что `@total_revenue`, `@orders_by_status`, `@top_products` работают
- Возможно проблема с `GROUP BY` в `@top_products` — уточнить

## 16. Адаптив
- Проверить и поправить CSS/Tailwind классы во всех страницах:
  - `frontend/app/page.tsx` — категории, акции
  - `frontend/app/catalog/page.tsx` — фильтры, сетка
  - `frontend/app/products/[id]/page.tsx` — изображения, описание
  - `frontend/app/cart/page.tsx` — таблица на мобильных
  - ERB-шаблоны админки

## 17. Валидация товара: перевод + дубликаты (app/views/admin/products/_form.html.erb)
- Ошибки валидации от Rails приходят на английском. Добавить русские переводы в `config/locales/ru.yml`
- Убрать дублирование сообщений об ошибках (сейчас `full_messages` могут дублироваться из-за бэка)

## 18. Подсветка пропущенных полей
- Добавить CSS-класс `border-red-500` или подобный для полей с ошибками валидации в админ-формах
- **Файлы**: `app/views/admin/products/_form.html.erb`, `app/views/admin/categories/_form.html.erb`, `app/views/admin/brands/_form.html.erb`

## 19. Колесико в списке товаров админки
- Возможно контейнер с `overflow-x-auto` блокирует скролл колесиком. Убрать или настроить правильно.
- **Файлы**: `app/views/admin/products/index.html.erb`

## 20. Стиль file input в админке
- В `app/views/admin/products/_form.html.erb` стр. 87: `class: "w-full"` — заменить на стилизованный вариант (CSS для `input[type="file"]` или кастомная кнопка)

## 21. Удалить столбец "В наличии" (app/views/admin/products/index.html.erb)
- Удалить `<th>Наличие</th>` и соответствующий `<td>` с `product.in_stock?`

## 22. SKU → Артикул (app/views/admin/products/index.html.erb)
- Поменять заголовок столбца с "SKU" на "Артикул"

## 23. Увеличить ширину таблицы (app/views/admin/products/index.html.erb)
- Добавить `min-w-[1200px]` или `w-full` с увеличенными `p-3` → `p-4`
- Увеличить `max-w-[1100px]` в `app/views/layouts/admin.html.erb` до `max-w-[1400px]`

## 24. Несколько фото товара
- `has_many_attached :images` уже есть, `multiple: true` в форме — должно работать
- Проверить, что `attach_images` не перезаписывает старые фото (сейчас `.attach` добавляет, а не заменяет — проверить)
- В админке: показывать все фото, дать возможность удалять отдельные

## 25. Блокировка удаления категорий/брендов с товарами
- В `Admin::CategoriesController#destroy` и `Admin::BrandsController#destroy` проверить `dependent: :restrict_with_error` или ручную проверку
- В модели: `has_many :products, dependent: :restrict_with_error`

## 26. Отмена заказа: возврат количества
- В `Admin::OrdersController#update`: при смене статуса на `cancelled` — увеличить `stock_quantity` на `order_items.quantity`
- Обновить `in_stock` если нужно

## 27. Таблица пользователей (app/views/admin/users/index.html.erb)
- Проверить, что таблица не разъезжается (возможно колонки слишком узкие)
- Добавить `overflow-x-auto`, увеличить ширину

## 28. Поиск в остальных страницах админки
- Добавить поиск по `q` в:
  - `Admin::OrdersController#index` (по email пользователя, ID заказа)
  - `Admin::UsersController#index` (по email)
  - `Admin::CategoriesController#index` (по имени)
  - `Admin::BrandsController#index` (по имени)

## 29. Удалить неиспользуемые ERB
- Проверить и удалить неиспользуемые/дублирующие ERB-шаблоны:
  - `app/views/catalog/index.html.erb` — если каталог только в Next.js
  - `app/views/products/show.html.erb` — если товар только в Next.js
  - Другие неиспользуемые

## 30. Letter Opener (config/environments/development.rb)
- `letter_opener` уже в Gemfile (стр. 80) и настроен (стр. 44-45)
- Проверить `bundle install` и что gem установлен
- Проверить, что работает сброс пароля через Devise и письма подтверждения

## 31. Расширить профиль (frontend + backend)
- Добавить поля: `first_name`, `last_name`, `phone`, `birth_date` в модель User
- Миграция + обновление валидаций
- Обновить форму профиля (`frontend/app/profile/page.tsx` + `app/views/profiles/edit.html.erb`)
- Обновить API (`Api::V1::ProfileController`)

---

## Приоритетность выполнения

### Блок 1: Критические баги (падает/не работает)
- 10 (сортировка по популярности)
- 15 (обзор в админке)
- 19 (колесико)
- 27 (таблица пользователей)

### Блок 2: Функциональные недостатки
- 3 (избранное в frontend)
- 6 (заказ не в наличии)
- 8 (наличие по размерам)
- 11 (отзыв без получения)
- 12 (отзыв из профиля)
- 13 (переход к товару из заказа)
- 14 (нет в наличии вниз)
- 24 (много фото)
- 25 (блокировка удаления)
- 26 (возврат при отмене)
- 28 (поиск в админке)
- 31 (расширить профиль)

### Блок 3: UI/UX улучшения
- 1 (фон категорий)
- 2 (промокод)
- 4 (переименовать раздел)
- 5 (размеры)
- 7 (перевод ошибок)
- 9 (сортировка размеров)
- 16 (адаптив)
- 17 (валидация товара)
- 18 (подсветка полей)
- 20 (стиль file input)
- 21-23 (таблица товаров админки)

### Блок 4: Чистка
- 29 (удалить ERB)
- 30 (letter_opener)