<% content_for :title, "Заказ №#{@order.id} — Магазин" %>

<h1 class="mt-0 text-2xl md:text-3xl">Заказ №<%= @order.id %></h1>

<div class="grid grid-cols-1 md:grid-cols-[1fr_320px] gap-4 md:gap-6 items-start">
  <div class="space-y-3 md:space-y-4">
    <% @order_items.each do |item| %>
      <div class="bg-white rounded-lg p-3 md:p-4 shadow-[0_2px_8px_rgba(0,0,0,0.06)] flex items-center gap-3 md:gap-4">
        <% if item.product.images.attached? %>
          <%= active_storage_image_tag item.product.images.first, resize_to_limit: [80, 80], alt: item.product.name, class: "w-14 h-14 md:w-20 md:h-20 object-cover rounded shrink-0" %>
        <% else %>
          <%= image_tag "https://placehold.co/80x80/ddd/333?text=#{CGI.escape(item.product.name[0..2])}", alt: item.product.name, class: "w-14 h-14 md:w-20 md:h-20 object-cover rounded shrink-0" %>
        <% end %>
        <div class="flex-1 min-w-0">
          <h3 class="mt-0 mb-1 font-semibold text-sm md:text-base"><%= link_to item.product.name, product_path(item.product), class: "text-accent no-underline hover:underline line-clamp-2" %></h3>
          <p class="text-xs md:text-sm text-[#888] m-0">Размер: <%= item.size %> | Количество: <%= item.quantity %></p>
          <p class="text-xs md:text-sm font-medium mt-1"><%= format("%.0f", item.price) %> ₽ за шт.</p>
        </div>
        <div class="font-bold text-sm md:text-base shrink-0"><%= format("%.0f", item.price * item.quantity) %> ₽</div>
      </div>
    <% end %>
  </div>

  <div class="bg-white rounded-lg p-4 md:p-5 shadow-[0_2px_8px_rgba(0,0,0,0.06)]">
    <h3 class="mt-0 mb-3 text-base md:text-lg">Информация о заказе</h3>
    <div class="space-y-2 text-sm">
      <div class="flex justify-between gap-2">
        <span class="text-[#888]">Статус:</span>
        <span class="font-medium text-right"><%= t("order.status.#{@order.status}") %></span>
      </div>
      <div class="flex justify-between gap-2">
        <span class="text-[#888]">Дата:</span>
        <span class="text-right"><%= l(@order.created_at, format: :long) %></span>
      </div>
      <div class="flex justify-between gap-2">
        <span class="text-[#888]">Адрес:</span>
        <span class="text-right max-w-[200px]"><%= @order.shipping_address %></span>
      </div>
      <div class="flex justify-between gap-2">
        <span class="text-[#888]">Доставка:</span>
        <span class="text-right"><%= @order.delivery_method_label %></span>
      </div>
      <div class="flex justify-between gap-2">
        <span class="text-[#888]">Оплата:</span>
        <span class="text-right"><%= @order.payment_method_label %></span>
      </div>
    </div>
    <div class="border-t border-[#eee] mt-3 pt-3 flex justify-between font-bold text-base md:text-lg">
      <span>Итого:</span>
      <span><%= format("%.0f", @order.total_amount) %> ₽</span>
    </div>
    <%= link_to "← Назад к заказам", orders_path, class: "inline-block mt-4 text-accent no-underline hover:underline text-sm" %>
  </div>
</div>
